name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: ubuntu:latest
    steps:
    - name: Run a one-line script
      run: wget -qO- https://raw.githubusercontent.com/takuyaw-w/dotfiles/master/install.sh | sh
